[package]
name = "poc_keypath"
version = "0.1.0"
edition = "2024"

[dependencies]
arc-swap = { version = "1.9.1", features = ["serde"] }
#rust-key-paths = { version = "3.1.1", features = ["arc-swap"] }
rust-key-paths = { version = "3.2.0-beta.1", features = ["nightly"] }

key-paths-derive = "3.0.2"
serde = { version="1.0.0", features=["derive"]}
serde_json = "1.0"
