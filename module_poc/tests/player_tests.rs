/*
// mod player;

#[cfg(test)]
mod tests {
    // Import the module to be tested
    use super::my_module;


    #[test]
    fn test_add() {

        // Use the functions from the imported module for testing
        // assert_eq!(my_module::add(2, 3), 5);
        // assert_eq!(my_module::add(-1, 1), 0);
    }
}
*/